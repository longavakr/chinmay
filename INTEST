#include<stdio.h>
int main()
{
    int n,i,k,j,count=0;
    scanf("%d",&n);
    scanf("%d",&k);
    for(i=0;i<n;i++)
    {
        scanf("%d",&j);
        if(j%k==0)
        {
            count++;
        }
    }
    printf("%d",count);
    return 0;
}
